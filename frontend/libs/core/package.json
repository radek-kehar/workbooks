{
  "name": "core",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "generate-index-files": "ctix single",
    "lint:check": "eslint ./src",
    "lint:fix": "eslint ./src --fix",
    "test": "vitest --run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
  },
  "devDependencies": {
    "ctix": "^1.8.1"
  }
}
